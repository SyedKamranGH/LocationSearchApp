/**
 * Interface for EmptyList component props.
 *
 * @interface EmptyListProps
 * @property {string} message - The message to display in the empty state
 */
export interface EmptyListProps {
  message: string;
}
